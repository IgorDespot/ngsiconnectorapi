const { expect } = require('chai');

const { singleValueCheck } = require("../../../components/rules");

describe("Function tests", function() {

    // it('should return somevalue', function () {
    //     expect(singleValueCheck(['id','someval'])).to.equal("someval");
    // });
});